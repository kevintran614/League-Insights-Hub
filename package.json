{
  "scripts": {
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm start --prefix backend",
    "test": "cd backend && npm run test",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\" \"npm run test\""
  }
}
