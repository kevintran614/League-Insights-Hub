class Account {
  constructor(gameName, tagLine, puuid, summonerData) {
    this.gameName = gameName;
    this.tagLine = tagLine;
    this.puuid = puuid;
    this.summonerData = summonerData;
  }
}

module.exports = Account;
